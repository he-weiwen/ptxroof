# The cross-entropy chunk at nanochat's vocabulary: two eight-trip
# loops, fully counted. Two PLAN.md presentation limitations show: every
# `$L__tmpN` debug label starts a block, and the reductions inlined from
# Triton's standard.py through gluon_ce.py:50 are attributed to
# standard.py (the block rows and the unrolled-source-lines list).
args = ["analyze", "{fixtures}/gluon/ce_chunk_kernel.sm_89.ptx"]
exit = 0
