# The same kernel with a left window of 768: the key loop's lower bound
# adds a branch; the trip count still depends on the CTA index.
args = ["analyze", "{fixtures}/gluon/attn_fwd_ws_kernel.w768.sm_89.ptx"]
exit = 0
