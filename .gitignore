# Cargo build artifacts. Cargo.lock IS committed (this repo builds a
# binary; the lock makes every build use identical dependency versions).
/target
# NVIDIA manuals fetched by tools/fetch-manuals.sh — cite, do not commit
/refs
# compile.py imports the Gluon fixture sources
__pycache__/
